rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permite leitura e escrita em todos os documentos e coleções
    match /{document=**} {
      allow read, write: if true;
    }
  }
}